
import { AppProviders } from '@/components/AppProviders';
import { AppRouter } from '@/components/AppRouter';

function App() {
  return (
    <AppProviders>
      <AppRouter />
    </AppProviders>
  );
}

export default App;
